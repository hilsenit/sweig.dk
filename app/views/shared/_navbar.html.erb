	<div class="container frontpage-navbar-wrapper">
		<div class="row">
			<% unless current_page?("/") %>
				<%= link_to root_path do %>
					<%= image_tag "lille-sweig-tilbage", class: "lille-sweig-tilbage" %>
				<% end %>
			<% end %>
			<div class="col-md-4 text-center">
				<%= link_to "NYT", nyt_path(), class: "#{if_active_helper(nyt_path())}" %>	
			</div>
			<div class="col-md-4 text-center">
				<%= link_to "LÆS", laes_path(), class: "#{if_active_helper(laes_path())}"%>	
			</div>
			<div class="col-md-4 text-center">
			<% if user_signed_in? %>
				<%= link_to "MIT", user_path(current_user.id), class: "#{if_active_helper( user_path(current_user.id))}" %> / <%= link_to "Log ud", destroy_user_session_path(), method: :delete, class: "navbar-log-ud-link" %>

			<% else %>
				<%= link_to "LOG-IN", benyt_path("log-in"), class: "#{if_active_helper( benyt_path("log-in"))}" %> 
				<span class="small">eller</span> 
				<%= link_to "OPRET", benyt_path("opret"), class: "#{if_active_helper( benyt_path("opret"))}" %>
			<% end %>
			</div>
		</div>	
	</div>
