<div class="col-lg-12 main">
	<%= render partial: "shared/search_header", locals: {site: "MAERKER"} %>
	<div class="row search-results-load hide-it">
		<%= image_tag "sweig-logo-load", class: "loading-icon" %>
	</div>

	<div class="row search-results mb-4">
		
	<div class="row my-4 py-4">
	<% @maerker.each do |m| %>
		<% next if count = m.works.count == 0 %>
		<%= link_to show_maerke_works_path(m.id), remote: true, class: "label-info float-left" do %>
	 		<span class="small tag-count"><%= m.works.count %></span> <%= m.title %>
		<% end %>
	 <% end %>
	 </div>
	 <div class="row"></div>
		 <div class="row mt-4 maerker-result">
		 </div>
	</div>
</div>

<script type="text/javascript">
	$(document).ready(function($) {
		$("#soegeFelt").on("keyup paste", function() {
			$(".find-headline").text("SØG");
			$(".underline").removeClass('underline');
			$(".search-results-load").show();
			$search_text = $(this).val();
			$.ajax({
				url: '/soegefelt/resultat',
				type: 'GET',
				data: {search: $search_text}
			});	
			$(".search-results-load").delay(800).fadeOut(300);
		});
	});

</script>