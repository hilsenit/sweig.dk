$(".search-results").html("<%= escape_javascript( render partial: 'search_result', locals: { works: @works, users: @users }) %><hr>");
$(".search-result .search-result p").each(function() {$search_text = "<%= @search %>";
	$text = $(this).val();
	debugger;
});