<% @questions.each do |question| %>
	<%= link_to not_logged_in_path("Hvis du gerne vil se spørgsmålets afstemning og kommentarer, skal du enten logge ind eller oprette dig.<br> Det er smertefrit og fedt."), remote: true, class: "questions-other question-wrapper row" do %>
		<div class="col-md-10">
			<u><%= question.headline %></u>
			<p class="lead">
			<%= truncate(question.text, length: 100) %>
			</p>
		</div>
		<div class="col-md-2 text-center">
			<span class="display-4"><%= question.votes.length%></span> <span class="small">har stemt</span>
		</div>

	<% end %>
<% end %>
