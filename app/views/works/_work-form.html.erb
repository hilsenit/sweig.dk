<%= form_for(:work) do |f| %>

		<%= f.label "Overskrift" %>
		<%= f.text_field :title %>

		<%= f.label "Tekst" %>
		<%= f.text_area :body %>

		<%= f.submit "Gem værk", class: "button primary" %>

<% end %>
  