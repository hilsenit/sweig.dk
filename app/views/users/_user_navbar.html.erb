
<div class="container-fluid">
	<div class="col-lg-12 text-center">
		<div class="border-image">
			<%= link_to "Opret værk", new_user_work_path(@user), class: "btn btn-secondary" %>
		</div>
	</div>
	<% if current_user %>
	<div class="row user-links-container">

		<div class="col-md-4">
		<%= link_to "Biblo", user_biblo_path(@user.friendly_id), class: "nav-link text-center modal-header-links #{if_active_link_helper user_biblo_path(current_user.friendly_id)}" %>
		</div>
		<div class="col-md-4">
		<%= link_to "SAMLING", user_saved_works_path(@user), class: "nav-link text-center modal-header-links #{if_active_link_helper user_saved_works_path(@user)}" %>
		</div>
		<div class="col-md-4">
		<%= link_to "Mine tekster", user_my_works_path(@user), class: "nav-link text-center modal-header-links #{if_active_link_helper user_my_works_path(@user)} #{"active-link" if current_page?(user_path(current_user.friendly_id))}" %>
		</div>

	</div>
	<% end %>
</div>
