<%= link_to not_logged_in_path("Hvis du gerne vil have mulighed for at benytte dig af FOELGER, så log in eller opret dig."), remote: true, class: "col-lg-8 foelger" do %>

<div class="col-lg-12 text-right">
	<%= @user.followers.count %> FOELGER <span class="small"><%= @user.username.parameterize %></span>
</div>
<div class="col-lg-12 text-right"> <span class="small"><%= @user.username.parameterize %></span> FOELGER <span class="small">selv</span> 
	<%= @user.following.count %>
</div>
<% end %>
<span class="border-image float-left foelger-img col-lg-4">
	<%= image_tag "sweig-plus", class: "foelger-img pt-1" %>
</span>
