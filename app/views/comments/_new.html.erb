<%= form_for [question, comment] do |f| %>
	<%= f.hidden_field :hidden_user, value: current_user.id %>
	<%= f.label :text, "Ny kommentar" %>
	<%= f.text_area :text, class: "form-control" %>
<% end %>
